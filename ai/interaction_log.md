# AI Interaction Log — Microfinance Loan Tracker (PRG2104 Final Project)

| # | Prompt Summary | AI Tool | Response Summary | What I Changed | File(s) |
|---|---|---|---|---|---|
| 1 | Requested first implementation covering all brief requirements | Claude | Built full ScalaFX/Scala 3 project: domain model, `Repository[T]`, CSV persistence, `Either` validation, 4 screens, README, UML | Not yet accepted — needed compile, review, and understanding before use | Whole project scaffold |
| 2 | Shared `sbt run` output with 11 compile errors | Claude | Fixed missing `scalafx.Includes` import, bad `ObservableBuffer.from` usage, ambiguous `cellFactory` setter across 4 files | Re-submitted after first version failed; verified fixes independently | MainApp, RegisterBorrowerScreen, DisburseLoanScreen, RepaymentsScreen |
| 3 | Shared 2nd `sbt run` output: 1 error + `-Wunused` warnings | Antigravity | Fixed unreachable `case l: Label` match, replaced deprecated `handle{}` syntax, removed unused imports, fixed unrendered `title` label bug | Kept `scalafx.Includes` in one file where removing it would break `onAction`; documented reasoning in code | MainApp, RegisterBorrowerScreen, DisburseLoanScreen, RepaymentsScreen, DashboardScreen, Validation |
| 4 | App ran successfully; asked about testing, truncated list text, decimal formatting, and remaining warnings | Antigravity | Widened list/window to fix truncation, formatted income to 2 decimals, removed now-unnecessary `scalafx.Includes`, confirmed JVM `[error]` logs were routine | Accepted fixes; confirmed prior `-Wunused` reasoning | RegisterBorrowerScreen, MainApp, DisburseLoanScreen, RepaymentsScreen |
| 5 | Asked why action buttons looked different; preferred lighter native blue | Claude | Reverted custom `.primary-button` CSS, kept only `defaultButton = true` for native light-blue look on all 4 buttons | Requested specific revert to native styling | RegisterBorrowerScreen, DisburseLoanScreen, RepaymentsScreen |
| 6 | Register screen not filling window width like other screens | Claude | Set `maxWidth = Double.MaxValue` and `Hgrow`/`Vgrow = Always` on containers/list to fix layout stretch | Accepted as delivered | RegisterBorrowerScreen |
| 7 | Requested "Mark Repaid" button be light red, others stay blue | Claude | Added `.light-red-button` CSS class; removed `defaultButton` from that button to avoid style conflict | Accepted as delivered | styles.css, RepaymentsScreen |
| 8 | Match Dashboard stats color to table header blue-grey; red button looked washed out | Antigravity | Changed `.stat-value` to `-text-muted` (#64748B); updated red button to clearer Material red (#FFCDD2/#E57373) | Accepted as delivered | styles.css |
| 9 | Reconsidered custom colors; wanted "Mark Repaid" back to standard blue | Claude | Removed `.light-red-button` class, restored `defaultButton = true` for consistency | Chose simplicity over custom color after iteration | RepaymentsScreen |
| 10 | Couldn't type into Group size field; focus color mismatch | Claude | Clarified field is disabled by design unless "Group" loan type selected; changed focus color to #5B9BD5 to match accent | Reported issue before checking Loan type dropdown state | styles.css |
| 11 | Wanted Borrower field to support typed entry + dropdown, sorted ascending | Antigravity | Made `ComboBox` editable with `StringConverter`, live filtering, ascending sort by borrower id | Supplied project zip + screenshot as basis for request | DisburseLoanScreen |
| 12 | Shared compile errors: `StringConverter` mismatch, `.text` unresolved, `setAll` rejection | Claude | Added `scalafx.Includes.*` import for implicit conversions; fixed buffer copy with `.toSeq*` | Supplied exact compiler output to target fix precisely | DisburseLoanScreen |
| 13 | Editable dropdown looked visually inconsistent with Loan type control | Antigravity | Explained editable ComboBox renders as split TextField+arrow; added scoped CSS for seamless pill styling | Flagged issue with screenshot | styles.css |
| 14 | Multiple rounds refining Borrower box height/border to match other fields | Antigravity | Iteratively fixed height/border; caught and fixed a transparency regression and missing `-fx-min-height` bug | Reported each discrepancy via annotated screenshots | styles.css |
| 15 | Wanted Borrower box to match Principal box exactly | Claude | Wrapped ComboBox in container using Principal's `.text-field` class directly; added focus listener for matching blue border | Flagged unreliability of incremental CSS matching, prompted more robust fix | DisburseLoanScreen, styles.css |
